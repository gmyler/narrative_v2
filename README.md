# Narrative Intelligence App v2

Replace your current `app.py` with this version.

This app reads CSV/JSON files from either:
- the GitHub repo root, or
- a `data/` folder.

New page added:
- Narrative Timeline

Run:
```bash
pip install -r requirements.txt
streamlit run app.py
```
